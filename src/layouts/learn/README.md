# layouts/learn

Page layouts of the learning area (DaviLearn). They reuse `styles/tokens.css` and the
components in `components/shared/` so both areas feel like a single site.

- Owner: UI Frontend Designer.

See `docs/architecture/project-structure.md`.
